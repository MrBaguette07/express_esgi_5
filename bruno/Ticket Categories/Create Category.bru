meta {
  name: Create Category
  type: http
  seq: 3
}

post {
  url: http://localhost:3000/ticket-categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "code": "reseau",
    "label": "Problème réseau"
  }
}
